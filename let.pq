// Exemplo de variáveis
let
    // Declarando variáveis
    nome = "João Silva",
    idade = 30,
    salario = 4500.50,
    ativo = true,
    
    // Usando as variáveis
    mensagem = "O funcionário " & nome & " tem " & Text.From(idade) & " anos."
in
    mensagem

    